function results(input1, input2, input3, input4, input5) {
  const total = input1 + input2 + input3 + input4 + input5;
}

  if(total <=5) {
    language = "ruby";
  } else if (total <=10) {
    language = "go";
  } else {
    langage = "swift";
  }
    return language;
}

